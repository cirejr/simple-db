import { DropdownMenu } from './dropdown-menu';

describe('DropdownMenu', () => {
  it('should create an instance', () => {
    const directive = new DropdownMenu();
    expect(directive).toBeTruthy();
  });
});
